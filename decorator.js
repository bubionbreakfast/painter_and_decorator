const Decorator = function(){
this.stock = []
};

Decorator.prototype.addPaintCan = function(paintCan){
  this.stock.push(paintCan)
};

Decorator.prototype.checkStock = function(){
  
  this.stock.litres{
    for (const can in stock)
    let total = 0
    const can += can[i]
    return can
  }
};

module.exports = Decorator
